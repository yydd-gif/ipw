# Electron's install script (@electron/get) downloads electron.exe from GitHub
# by default. This mirror is read as npm_config_electron_mirror so Chinese
# networks can complete `npm install` without hand-setting ELECTRON_MIRROR.
electron_mirror=https://npmmirror.com/mirrors/electron/
