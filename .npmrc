# @electron/get reads npm_config_electron_mirror during Electron's install script.
# Pin the China mirror so `npm install` can fetch the binary without a hand-set env var.
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
